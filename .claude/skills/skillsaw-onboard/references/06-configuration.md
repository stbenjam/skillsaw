# Create configuration

If `.skillsaw.yaml` does not exist, run `skillsaw init` and tell the user this
file controls rule settings. Preserve an existing configuration unchanged.

Record whether a file was created, then return to the router.
