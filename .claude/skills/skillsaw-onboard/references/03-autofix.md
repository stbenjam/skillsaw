# Step 3: Run deterministic autofixes

Run `skillsaw fix` to apply safe deterministic fixes, then lint again. Tell the
user how many violations were fixed and how many remain; retain those counts
for Step 10.

If violations remain, return to the router and read Step 4. Otherwise skip
Steps 4–5 and read Step 6.
