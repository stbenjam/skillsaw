# Step 2: Run the initial scan

Run `skillsaw tree` with the command prefix chosen in Step 1. Summarize the
detected repository types, instruction files, plugins, skills, and other
components for the user.

Run the linter and capture its complete output. Record the initial error and
warning counts for the final summary. If violations exist, return to the
router and read Step 3. If none exist, skip Steps 3–5 and read Step 6.
