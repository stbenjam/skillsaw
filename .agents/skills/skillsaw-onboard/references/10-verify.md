# Step 10: Run final verification

Run the linter once more and confirm it exits successfully. Summarize:

- initial error and warning counts;
- deterministic and manual fix counts;
- baselined count;
- CI, Makefile, and badge choices;
- every file created or modified.

Remind the user to commit every applicable artifact, including
`.skillsaw.yaml`, `.skillsaw-baseline.json`, `.skillsaw-badge.json`, README
changes, CI workflows, `.gitlab-ci.yml`, Makefile changes, and edited context
files.
