# Step 7: Set up CI

Ask which CI system the repository uses. Apply only the selected section.

## GitHub Actions

Resolve current commit SHAs before writing workflows:

```console
git ls-remote --tags https://github.com/actions/checkout.git v5
git ls-remote --tags https://github.com/stbenjam/skillsaw.git v0
```

Create `.github/workflows/lint.yml`, using those SHAs with trailing version
comments:

```yaml
name: Lint

on:
  pull_request:
  push:
    branches: [main]

permissions:
  contents: read

jobs:
  skillsaw:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@<CHECKOUT_SHA> # v5
        with:
          persist-credentials: false
      - uses: stbenjam/skillsaw@<SKILLSAW_SHA> # v0
        with:
          strict: true
```

Offer PR review comments. If accepted, add
`.github/workflows/lint-review.yml`:

```yaml
name: Lint Review

on:
  workflow_run:
    workflows: ["Lint"]
    types: [completed]

jobs:
  review:
    if: github.event.workflow_run.event == 'pull_request'
    runs-on: ubuntu-latest
    permissions:
      pull-requests: write
    steps:
      - uses: stbenjam/skillsaw/review@<SKILLSAW_SHA> # v0
```

Offer the dedicated weekly dead-link check. If accepted, create
`.github/workflows/link-check.yml` from the
[scheduled link-check recipe](https://skillsaw.org/ci/#scheduled-external-link-checking).
Keep its `name: link-check`, weekly schedule, manual trigger,
`contents: read`, `issues: write`, and `skillsaw-link-check` concurrency group.
Reuse the resolved SHAs and invoke:

```yaml
      - uses: stbenjam/skillsaw/link-check@<SKILLSAW_SHA> # v0
```

The Action owns the network check and upserts one issue for confirmed `404`
and `410` responses. It never auto-closes the issue because silent network
failures are inconclusive.

## GitLab CI

Add a `skillsaw` job to `.gitlab-ci.yml` so violations appear in merge-request
Code Quality results:

```yaml
skillsaw:
  image: ghcr.io/stbenjam/skillsaw:latest
  stage: test
  script:
    - skillsaw --strict --output gitlab:gl-code-quality.json
  artifacts:
    reports:
      codequality: gl-code-quality.json
```

## No CI

If the user declines CI, leave it unchanged and point them to the
[CI documentation](https://skillsaw.org/ci/).

Record the selected setup, then return to the router and read Step 8.
