# Agent instructions

`services/api` is the Python backend and `apps/web` is the front end. Cursor
reads this file for portable instructions; the `.cursor/rules` files below
carry the Cursor-specific activation metadata.

Run `make test` before opening a pull request.
