---
description: Conventions for the Python API service
globs:
  - "services/api/**/*.py"
alwaysApply: false
---

# API service

Every endpoint returns a Pydantic model, never a bare dict.

Database sessions are opened with the `session_scope()` context manager.
