# note-taker

The catalog registers this directory as a local plugin source, but its
`.codex-plugin` is a symlink pointing out of the checkout — the manifest
Codex would install belongs to another tree entirely.

## Install

```
codex plugin install note-taker
```
