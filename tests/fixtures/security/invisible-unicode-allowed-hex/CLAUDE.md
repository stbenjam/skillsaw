# Project instructions

This documentation site is generated from markdown sources that use
zero-width spaces for line-break hints in long identifiers like
`VeryLongComponent​RegistryFactory` — do not strip them.

## Conventions

- Run `make docs` after editing any file under `docs/`.
- Keep headings in sentence case.
- Wrap prose at 80 columns.
