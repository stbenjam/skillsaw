# AI Harness

This project provides a financial analysis AI agent with red-team eval coverage.

## Development

- Run `make test` to execute the test suite
- Run `make eval` to run promptfoo evaluations against the local agent
