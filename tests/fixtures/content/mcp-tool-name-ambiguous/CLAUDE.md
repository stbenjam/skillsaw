# Issue triage

Use the gateway tool `mcp__gateway__mcp__jira__getIssue` to read the ticket.
Use `mcp__internal__report__generate` to prepare its summary.
Keep the ticket details with the report so the reviewer can compare both.
