# Billing service

Keep the accounting calculations in pure Python functions so they can be
checked without opening a database connection. Run the unit checks before
changing the behavior of a calculation.
