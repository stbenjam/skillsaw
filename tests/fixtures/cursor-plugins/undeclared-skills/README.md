# release-kit

Cursor loads the skills under `skills/`. The skills under `shared/skills/`
are installed into other agents with `npx skills add`, so Cursor does not
load them, but they follow the same Agent Skills layout.
