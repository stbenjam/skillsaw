# commit-guard

Keeps agent commits formatted. Claude Code runs the hooks in
`hooks/hooks.json` before shell commands and file edits; Cursor loads the
formatting rule in `rules/`.

Install it from the team marketplace, then run the formatter once so the
first commit starts clean.
