# Testing

Every exported function in `services/ingest` has a table-driven test.

<!-- skillsaw-assert content-weak-language -->
You should probably try to make sure the tests work correctly before pushing.
