# Toolchain sandbox

A scratch workspace for trying out build toolchains. Nothing here ships.

## Conventions

- Keep experiments in a dated directory under `spikes/`.
- Delete a spike once its findings land in an ADR.
