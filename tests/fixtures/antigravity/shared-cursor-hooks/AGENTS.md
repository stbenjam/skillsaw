# Ferrymark

Ferrymark checks ferry timetables and publishes timetable validation reports.
Run `make test` before proposing timetable changes. Keep all times in UTC.
Cursor and Antigravity share the repository's hook configuration through
symlinks so maintainers can inspect the same automation in either editor.
