# Allocation fields

| Field | Meaning |
| --- | --- |
| `route_id` | The route the sailing belongs to, at most 32 bytes. |
| `berth_id` | The berth the sailing is assigned to. |
| `superseded_by` | The row that replaces this one, or null. |
