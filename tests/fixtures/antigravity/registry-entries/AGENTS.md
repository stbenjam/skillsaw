# Ferrymark

The scheduling monorepo for a coastal ferry operator.

Run `make test` before pushing. A published timetable row is immutable —
a correction writes a superseding row rather than editing the original.
