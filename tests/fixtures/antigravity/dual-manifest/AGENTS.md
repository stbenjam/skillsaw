# Routeboard

Routeboard validates transit route definitions before publication.
Run `make test` after changing the route planner or its plugin configuration.
Keep route identifiers stable so downstream timetable exports remain compatible.
