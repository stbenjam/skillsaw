# Berth tools

This project validates berth allocations and audits timetable changes.
Run `make lint` for configuration changes and `make test` for allocation logic.
The editor and the installed plugin share the plugin's hooks configuration.
