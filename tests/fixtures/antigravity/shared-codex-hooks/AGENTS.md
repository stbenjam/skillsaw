# Ferrymark

Ferrymark validates ferry timetables before publishing them.
Run `make test` before proposing a timetable change and use UTC timestamps.
The Codex plugin declares the hooks file in the shared agent directory.
Keep hook changes reviewable alongside the timetable checks they run.
