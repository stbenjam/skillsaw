# Harbourworks

Codex plugin catalog for the harbour team. The catalog and its one local
plugin live under `.agents/plugins/`, the location Codex publishes from.

## Build and test

- `make test` runs the unit suite.
