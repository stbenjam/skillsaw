All Python code must use 4-space indentation and follow PEP 8 naming
conventions. Function names use snake_case, class names use PascalCase.

Import ordering: standard library first, then third-party, then local.
Separate each group with a blank line.

Maximum line length is 100 characters. Use `black` to enforce this.
