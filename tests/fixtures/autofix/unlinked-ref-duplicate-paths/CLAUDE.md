# Development Setup

Before making changes, always run scripts/test.py to verify the test suite passes.

## Making Changes

After editing any source files, re-run scripts/test.py to catch regressions early.
