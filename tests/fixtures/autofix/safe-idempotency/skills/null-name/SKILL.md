---
name:
description: Skill whose name key exists but holds a null value
---

# Null Name Skill

This skill has a `name:` key with no value (YAML null). The fix must replace
the value in place rather than prepend a duplicate key.
