# Style rules

Cline loads every file in this directory into the system prompt.

- Prefer small commits that each leave the test suite green.
- Import modules, not individual names, in Python.

<!-- skillsaw-assert content-weak-language -->
Be careful when touching the shared database models.
