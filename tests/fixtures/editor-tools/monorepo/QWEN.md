# Qwen Code instructions

## Environment

Activate the project virtualenv at `.venv` before running any Python command.
Never invoke the system `python3` — it does not have the project installed.

## Database access

<!-- skillsaw-assert content-weak-language -->
Be careful when writing migrations against the production database.
