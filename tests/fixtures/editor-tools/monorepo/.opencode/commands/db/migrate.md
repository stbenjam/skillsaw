---
description: Generate and apply a database migration
---

OpenCode names this command `db/migrate` after its path, so the loader
reads `commands/` recursively.

<!-- skillsaw-assert content-weak-language -->
You should probably review the generated SQL before applying it.

Run `./scripts/migrate.sh` and report the new revision.
