---
description: Breaks an issue into reviewable steps. Use when a change is too large to land in one commit.
---

Break the issue into steps small enough that each one leaves the suite
green.

<!-- skillsaw-assert content-weak-language -->
You should probably read the ADRs under `docs/adr/` first.

Stop at the step list.
