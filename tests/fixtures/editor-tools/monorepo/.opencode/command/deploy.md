---
description: Deploy the current branch to the staging environment
---

Check that CI is green on this branch before doing anything else.

<!-- skillsaw-assert content-weak-language -->
You should probably confirm the migration plan first.

Run `./scripts/deploy.sh staging` and report the resulting revision.
