# Review the working tree

Read the output of `git diff --staged` and report problems you find.

## Steps

1. Summarise what the change does in one sentence.
2. Flag any missing test coverage for new branches.
3. Stop and report if the diff touches authentication code.

<!-- skillsaw-assert content-weak-language -->
Consider using the existing fixtures where a new test needs sample data.
