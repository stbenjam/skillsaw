# berth-notes

Handover notes for the berth roster, written as slash commands so the
outgoing watch can run them without leaving the session.

There is no manifest here yet, and nothing else the installer recognizes.
