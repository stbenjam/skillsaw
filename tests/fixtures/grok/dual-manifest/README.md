# tide-charts

One plugin directory, two manifests: `.grok-plugin/plugin.json` for Grok
Build and `.claude-plugin/plugin.json` for Claude Code. Both hosts read the
same `commands/`, `hooks/hooks.json` and `.mcp.json` beside them.

## Install

```
grok plugin install tide-charts --trust
```
