# tide-charts

Turn NOAA tide predictions into shoreline survey windows.

Installed from the `harbour-plugins` marketplace:

```
grok plugin install tide-charts --trust
```

The skill needs a NOAA CO-OPS station id and a date range; everything else
it derives.
