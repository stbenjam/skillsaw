# harbour telemetry plugins

Two plugins that keep their hooks and MCP servers in the manifest rather
than in the conventional files: `buoy-watch` writes them inline, `gate-log`
points at files of its own naming.
