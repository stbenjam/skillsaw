# buoy-watch

Flag buoy readings that drift from the station's rolling median.

The manifest carries the session hook and the `buoys` MCP server inline, so
there is no `hooks/hooks.json` or `.mcp.json` to keep in sync with it.
