# gate-log

Reconcile lock-gate openings against the shift log at the end of a session.

Both files live under `config/`, named in the manifest rather than at the
conventional paths, so the plugin keeps one directory for everything it
configures.
