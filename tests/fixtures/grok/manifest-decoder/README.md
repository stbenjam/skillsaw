# Migration tools

This plugin provides a migration review skill and a reference documentation
server. Use the skill to assess schema compatibility before rolling out readers
that depend on new columns.
