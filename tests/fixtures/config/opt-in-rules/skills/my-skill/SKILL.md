---
name: my-skill
description: A skill with an unrecognized subdirectory and no evals, plus a deliberately long-winded description that keeps going well past the soft budget so the opt-in description length rule has something to warn about when it is explicitly enabled in the skillsaw configuration file for this fixture.
---

# My Skill

This skill intentionally has an unrecognized `extras/` subdirectory
and is missing the `evals/evals.json` file.
