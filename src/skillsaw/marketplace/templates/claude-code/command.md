---
description: "Runs the {{COMMAND_NAME}} workflow. Replace this description with what the command does."
argument-hint: "[args]"
---

## Name
`{{PLUGIN_NAME}}:{{COMMAND_NAME}}`

## Synopsis
```text
/{{PLUGIN_NAME}}:{{COMMAND_NAME}} [args]
```

## Description
Update this section to explain what the `/{{PLUGIN_NAME}}:{{COMMAND_NAME}}`
command does and when to run it.

## Implementation

1. **Parse Arguments**: Read the arguments from the command input
2. **Execute**: Run the main operation and check the result
3. **Return Response**: Write the result back to the user

## Return Value

- **Format**: Text
- **Example**: Update with sample output, e.g. `Completed {{COMMAND_NAME}}`

## Examples

1. **Basic usage** — run `/{{PLUGIN_NAME}}:{{COMMAND_NAME}}` with no arguments:
   ```text
   /{{PLUGIN_NAME}}:{{COMMAND_NAME}}
   ```

## Arguments

- `$1`: (Optional) Update with what the first argument controls
